# ADO Pipeline Viewer

A VS Code extension that visualizes Azure DevOps YAML pipelines as interactive flowcharts with full recursive template resolution.

## Features

- Renders pipeline stages as a directed graph with orthogonal connectors
- Recursively resolves `template:` references (stages, jobs, steps) across repositories
- Expand/collapse stages to inspect jobs and individual steps
- Click template steps to navigate into their resolved templates
- Displays conditions, parameters, inputs, and `continueOnError` at every level
- Parameter evaluation for `${{ if }}` conditional blocks with `eq`/`ne`/`and`/`or`
- Color-coded stage types (Build, Deploy, Validate, Detect, Sync, etc.)
- Color-coded step types (VSBuild, NuGet, PowerShell, Bash, SonarQube, Cmd, etc.)
- Inline script expansion on click (independent of Expand/Collapse All)
- Zoom controls and responsive layout

## Prerequisites

- **Node.js 18+** (recommend using [nvm-windows](https://github.com/coreybutler/nvm-windows))
- **VS Code 1.80+**

## Build from Scratch

### 1. Install Node.js via nvm-windows

If you don't have nvm-windows installed:

```powershell
# Download and install nvm-windows from https://github.com/coreybutler/nvm-windows/releases
# Then:
nvm install 18
nvm use 18
node --version   # Should show v18.x.x
```

### 2. Clone and install dependencies

```powershell
cd c:\Git\KMN\PipelineViewer
npm install
```

### 3. Compile

```powershell
npm run compile
```

This runs `esbuild` to bundle `src/extension.ts` into `out/extension.js`.

### 4. Package as VSIX

```powershell
npm run package
```

This produces `ado-pipeline-viewer-0.1.0.vsix` in the project root.

### 5. Install the VSIX

```powershell
code --install-extension ado-pipeline-viewer-0.1.0.vsix --force
```

Then reload VS Code (`Ctrl+Shift+P` > `Developer: Reload Window`).

## Quick Deploy (Development)

For rapid iteration without re-packaging:

```powershell
npm run compile
Copy-Item "out\extension.js" "$env:USERPROFILE\.vscode\extensions\pires11d.ado-pipeline-viewer-0.1.0\out\extension.js" -Force
```

Then reload VS Code.

## Usage

1. Open any `.yml` or `.yaml` file in VS Code
2. Right-click the file in the Explorer > **Visualize ADO Pipeline**
3. Or click the flowchart icon in the editor title bar

## Configuration

In VS Code settings (`settings.json`):

```json
{
  "adoPipelineViewer.templateRepositoryMappings": {
    "templates": "C:/Git/MyOrg/MyTemplateRepo"
  },
  "adoPipelineViewer.maxTemplateDepth": 10
}
```

- **templateRepositoryMappings**: Maps `resources.repositories` aliases to local paths on disk. If empty, the extension auto-detects sibling folders.
- **maxTemplateDepth**: Maximum recursion depth for template resolution (default: 10).

## Project Structure

```
PipelineViewer/
  src/
    extension.ts       - Extension entry point, command registration
    pipelineParser.ts  - YAML parser, template resolver, condition evaluator
    webviewPanel.ts    - Webview panel with HTML/CSS/JS rendering
  out/
    extension.js       - Bundled output (generated by esbuild)
  package.json         - Extension manifest
  esbuild.js          - Build configuration
  .vscodeignore       - Files excluded from VSIX package
```

## Scripts

| Command | Description |
|---------|-------------|
| `npm run compile` | Build the extension (one-shot) |
| `npm run watch` | Build and watch for changes |
| `npm run package` | Create `.vsix` package |

## Troubleshooting

**Two icons showing for the command:**
You have duplicate extension folders. Check `~/.vscode/extensions/` for both `local.ado-pipeline-viewer-*` and `pires11d.ado-pipeline-viewer-*`. Remove the `local` one and reload.

**Templates not resolving:**
Ensure the template repository is cloned locally and either:
- Lives in a sibling folder to the pipeline's repo, OR
- Is explicitly mapped in `adoPipelineViewer.templateRepositoryMappings`

**Extension not activating:**
The extension only activates on `.yml` and `.yaml` files. Open one first.
