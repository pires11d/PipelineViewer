- Remove Stage and Job outline when the file is a step template;
- Let's fix the file C:\Git\KMN\Tools\Pipelines\DartTemplates\Dart.PipelineTemplates\templates\stages\backend\backend-backfill-databases-stage.yml so that it will not have multiple stages. This is completely wrong, it should not be something like that, we might as well go back to the pipelines/backfill-environments-pipeline.yml layout then, and from there, never call a template that has more than a single stage in it, therefore calling those stages directly from the pipeline template file, as it is already doing for the Frontend scenario.
- Oh, and what about these Unnammed pipelines, for templates, there's no sense in having that field filled. it actually should have a smaller font, be in bold style as it is, but the same size of the other ones used in that section. But, as you make that font smaller, we could indeed benefit from having that exact Font size and style for another field, which is way more important: THE FILE NAME ITSELF! But now I see, there is already a field for the "Source"... first of all, that font size is ridiculously small, and second of all, there is no way I was going to guess that source is the same as File Name. Why not keep it simple, bruv? So to conclude: Switch places between the damn "Source: filename.yml" and "Unnamed Pipeline" fields, and adjust the labels to something that feels more obvious, please.